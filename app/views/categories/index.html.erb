<h1>Category</h1>
<%= link_to  "New category",  new_restro_category_path %>

<table>
    <% @categories.each do |category| %>
        <tr>
        <td><%= link_to category.name, [category.restro, category] %></td>
        </tr>
    <% end %>
</table>