<h1>
<%= @restro.name %>
<%= link_to 'Add new category', new_restro_category_path(@restro), class: 'btn btn-warning' %>
</h1>
<table>
    <% @restro.categories.each do |category| %>
        <tr>
        <td><%= link_to category.name, [category.restro, category] %></td>
        </tr>
    <% end %>
</table>